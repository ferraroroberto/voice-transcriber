# Test-only dependencies. Install with:
#   .venv\Scripts\python.exe -m pip install -r requirements-dev.txt
#
# Runtime deps stay in requirements.txt — this file is opt-in so the
# tray/whisper deployment image doesn't ship test plumbing.

pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12
# FastAPI TestClient uses httpx under the hood.
httpx>=0.27

# Playwright browser-E2E smoke tests (tests/e2e). After install:
#   .venv\Scripts\python.exe -m playwright install chromium
pytest-playwright>=0.5
